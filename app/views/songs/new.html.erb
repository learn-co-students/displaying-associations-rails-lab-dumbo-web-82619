<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_id %>
  <%= f.collection_select :artist_id, @artists, :id, :name, prompt: true %><br>
  <br/>
  <%= f.submit %>
<% end %>


